# MACAF experiment results

```text
base/
  v1/{training,sweep}
  v2/{training,sweep}
  v3/{training,sweep}
kd_old_swin/{training,sweep}
self_kd/
kd_clean_swin/{training,sweep}
teacher_sanity/
```

Training logs, batch logs, validation trade-offs, and sweep outputs include their
experiment/version in the filename. Historical duplicate bundles are nested under
`archive_*` directories and are not canonical manuscript inputs.
