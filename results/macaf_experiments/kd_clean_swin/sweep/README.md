# Clean Swin KD sweep

The canonical 15-subset evaluator writes descriptive CSV/JSON pairs here:

`test_<subset>_macaf_clean_swin_kd_sweep.{csv,json}`

The previously cancelled output directory was removed; no partial sweep files were retained.
